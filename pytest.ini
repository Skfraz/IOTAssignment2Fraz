[pytest]
asyncio_mode = auto
testpaths = tests
# AMQP tasks were ignored for this submission, so the AMQP tests (which also
# require a running RabbitMQ broker + pika) are excluded from collection.
addopts = --ignore=tests/amqp
timeout = 30
log_cli = true
log_cli_level = INFO
